name: Hello Bot

on:
    push:

jobs:
    say-hello:
        # os
        runs-on: ubuntu-latest
        # steps
        steps:
            - name: Print a message
              run: echo "Hello From Hello-Bot"

